### hello.cpp ###
g++ -Wall -std=c++23 -o hello hello.cpp
./hello

### sim.cpp ###
g++ -std=c++23 -O2 -Iinclude -o sim src/*.cpp
./sim kernels/kernel1.kerr

### Visual Studio ###
mkdir build
cd build
cmake .. -G "NMake Makefiles" -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release
.\gpu-sim.exe ../kernels/kernel1.kerr
